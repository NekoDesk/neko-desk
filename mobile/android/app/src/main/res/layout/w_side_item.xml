<?xml version="1.0" encoding="utf-8"?>
<!-- 어제·내일 칸의 한 줄 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="2dp"
    android:paddingBottom="2dp">

    <TextView android:id="@+id/i_chk"
        android:layout_width="12dp" android:layout_height="12dp"
        android:background="@drawable/w_check_off" android:gravity="center"
        android:textSize="8sp" android:textColor="#FFFFFF"
        android:includeFontPadding="false" android:layout_marginEnd="6dp" android:text="" />

    <TextView android:id="@+id/i_text"
        android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1"
        android:textSize="10sp" android:textColor="#5F6368"
        android:maxLines="1" android:ellipsize="end" android:text="" />
</LinearLayout>
